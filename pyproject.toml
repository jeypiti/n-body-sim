[tool.black]
line-length = 100
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 80
lines_after_imports = 2
remove_redundant_aliases = true
float_to_top = true
color_output = true